public interface EmployeePosition {
    public String getJobTitle(); //возвращает название занимаемой должности
    public double calcSalary(double baseSalary); //возвращает зарплату сотрудника
}
